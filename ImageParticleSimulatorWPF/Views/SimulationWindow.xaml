<Window x:Class="ImageParticleSimulatorWPF.Views.SimulationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:ImageParticleSimulatorWPF.Views"
        Title="Beauty in Chaos"
        Background="#09090b"
        MinWidth="360"
        MinHeight="300"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Storyboard x:Key="SparkleAnimation" RepeatBehavior="Forever" AutoReverse="True">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1" To="0.3" Duration="0:0:1.2"/>
            <DoubleAnimation Storyboard.TargetProperty="Width"
                             From="6" To="9" Duration="0:0:1.2"/>
            <DoubleAnimation Storyboard.TargetProperty="Height"
                             From="6" To="9" Duration="0:0:1.2"/>
        </Storyboard>
    </Window.Resources>

    <Grid Margin="14">
        <views:ParticleCanvas x:Name="BallCanvas"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Top"
                              Balls="{Binding Balls}"
                              ActiveBallCount="{Binding ActiveBallCount}" />

        <Grid x:Name="Overlay" Background="orange" Visibility="{Binding IsOverlayVisible, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="✨ Assembling the Magic... ✨"
                       Foreground="Black"
                       FontFamily="Consolas"
                       FontSize="22"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center">
                <TextBlock.RenderTransform>
                    <TranslateTransform Y="0"/>
                </TextBlock.RenderTransform>
                <TextBlock.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)"
                                                 From="0" To="-10" Duration="0:0:0.8" />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </TextBlock.Triggers>
            </TextBlock>
        </Grid>

        <Border HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0 10 10 0"
                Padding="12 8"
                Background="#C0111726"
                CornerRadius="12">
            <StackPanel>
                <TextBlock Text="{Binding CurrentFps, StringFormat={}{0:0} FPS}"
                           Foreground="White"
                           FontWeight="SemiBold"/>
                <TextBlock Text="{Binding ActiveBallCount, StringFormat=Particles: {0}}"
                           Margin="0 2 0 0"
                           Foreground="#d1d5db"
                           FontSize="11"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
